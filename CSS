```css
#puzzle {
width: 300px;
height: 300px;
display: grid;
grid-template: repeat(3, 1fr) / repeat(3, 1fr);
gap: 2px;
}
.tile {
display: flex;
align-items: center;
justify-content: center;
font-size: 2rem;
background: #ccc;
cursor: pointer;
transition: all 0.2s;
}
.empty {
background: white;
cursor: default;
}
